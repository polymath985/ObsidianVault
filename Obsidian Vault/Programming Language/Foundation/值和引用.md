---
tags:
  - CPP
  - CSharp
aliases:
  - 引用
  - 值
---
# 值
- 储存位置：栈
- 在函数传参和赋值操作时，值通常进行复制操作，即传入副本，修改副本不会影响原始值的数据
# 引用
 - 储存位置：地址：栈，值：堆
 - 在函数传参和赋值操作时，引用通常将储存在栈中的地址进行复制，使两个不同变量指向的堆内容一致，修改新变量也会导致原始变量的修改。（即浅拷贝）
 - 但是，在某些语言的某些引用，如[[string]]类型，可能会在直接赋值时进行深拷贝操作。